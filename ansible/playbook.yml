---
- name: Install Jenkins
  hosts: Jenkins
  user: ubuntu
  become: true
  roles:
    - jenkins
  
- name: Install Docker and Ansible
  hosts: Docker
  user: ubuntu  
  become: true
  vars_files:
    - ./docker/variables/main.yml  
  roles:
    - docker
    - ansible